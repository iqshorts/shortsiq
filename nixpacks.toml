[phases.setup]
nixPkgs = ["ffmpeg", "yt-dlp", "python312"]

[phases.install]
cmds = [
  "python3.12 -m venv /app/venv",
  "/app/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "/app/venv/bin/python mcp_server.py"
